// The selectors are tested alongside the testing of the components.
// The reason is that in this instance, the selectors contain too little logic to be worth tested by themselves.

describe('Contact Selectors', () => {
  it('should select the feature state', () => {
    
  });
});
